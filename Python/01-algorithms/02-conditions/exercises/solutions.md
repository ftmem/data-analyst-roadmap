# Solutions

## تمرین ۱ — مثبت یا غیرمثبت

```text
Start
↓
Input A
↓
If A > 0?
↙          ↘
Yes         No
↓            ↓
Output       Output
Positive     Not Positive
↘            ↙
    End
```

---

## تمرین ۲ — زوج یا فرد

```text
Start
↓
Input A
↓
If A % 2 == 0?
↙             ↘
Yes            No
↓               ↓
Output Even     Output Odd
↘               ↙
      End
```

---

## تمرین ۳ — بزرگ تر بین دو عدد

```text
Start
↓
Input A
↓
Input B
↓
If A > B?
↙          ↘
Yes         No
↓            ↓
Output A     Output B
↘            ↙
    End
```

---

## تمرین ۴ — قبولی یا مردودی

```text
Start
↓
Input Grade
↓
If Grade >= 10?
↙              ↘
Yes             No
↓                ↓
Output Pass      Output Fail
↘                ↙
       End
```

---

## تمرین ۵ — مثبت، منفی یا صفر

```text
Start
↓
Input A
↓
If A > 0?
↙          ↘
Yes         No
↓            ↓
Output       If A < 0?
Positive     ↙       ↘
           Yes        No
            ↓          ↓
         Output      Output
         Negative     Zero
             ↘        ↙
                End
```
